# Local development uses a file-based SQLite database via the libSQL adapter.
DATABASE_URL="file:./dev.db"

# For a hosted libSQL / Turso database (e.g. in production), set these instead:
# DATABASE_URL="libsql://your-database.turso.io"
# DATABASE_AUTH_TOKEN="your-turso-auth-token"
